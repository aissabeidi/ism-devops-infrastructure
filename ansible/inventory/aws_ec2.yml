plugin: amazon.aws.aws_ec2

regions:
  - eu-west-3

filters:
  tag:Project: memoire-lan-security
  instance-state-name: running

keyed_groups:
  - key: tags.Name
    prefix: ""
    separator: ""

hostnames:
  - ip-address

compose:
  ansible_host: public_ip_address
  ansible_user: "'ubuntu'"
